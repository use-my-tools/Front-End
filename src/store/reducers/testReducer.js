const initialState = {};

export default function testReducer(state = initialState, action) {
  switch (action.type) {
    // case 'ACTION_TYPE':
    //   return code
    default:
      return state;
  }
}
